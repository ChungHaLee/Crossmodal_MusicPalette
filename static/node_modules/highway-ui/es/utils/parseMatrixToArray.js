/**
 *
 * @param matrixObj
 * @returns [1,0,0,1,0,0]
 */
function parseMatrixToArray(matrixObj) {
  var scaleX = matrixObj.scaleX,
      skewY = matrixObj.skewY,
      skewX = matrixObj.skewX,
      scaleY = matrixObj.scaleY,
      translateX = matrixObj.translateX,
      translateY = matrixObj.translateY;
  var matrixArr = [scaleX, skewY, skewX, scaleY, translateX, translateY];
  return matrixArr;
}

export default parseMatrixToArray;